name: child1
on: 
  workflow_call:
    inputs:
      name:
        type: string
        required: true
    outputs:
      name_out: ${{ jobs.job.outputs.message }}
jobs:
  job:
    runs-on: ubuntu-latest
    outputs: 
      message: ${{ steps.runtask.outputs.firstout }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master
      - name: task
        id: runtask
        run: |
          echo ${{inputs.name}}
          ls -ltr
          echo "firstout=hello output" >> $GITHUB_OUTPUT