name: child1
on: 
  workflow_call:
    inputs:
      name:
        type: string
        required: true
jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master
      - name: task
        run: |
          echo ${{inputs.name}}
          ls -ltr