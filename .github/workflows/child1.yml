name: child1
on: 
  workflow_call:
    inputs:
      name:
        required: true
        type: string
jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master
      - name: task
        run: |
          echo ${{name}}